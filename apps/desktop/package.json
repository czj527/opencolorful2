{
  "name": "@agentplant/desktop",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "M1 入口：Electron 桌面壳（渲染层复用 @agentplant/web 的构建产物）",
  "main": "dist/main/main.cjs",
  "scripts": {
    "build": "node scripts/clean.mjs && tsc -p tsconfig.main.json && node scripts/copy-assets.mjs && node scripts/gen-build-info.mjs",
    "typecheck": "tsc -p tsconfig.main.json --noEmit",
    "dev": "electron .",
    "start": "electron .",
    "package": "electron-builder --dir",
    "package:release": "electron-builder"
  },
  "devDependencies": {
    "electron": "33.2.1"
  }
}
